name: godesk
scripts:
  - path: magefiles
    sdk: go
extensions:
  - path: magefiles/vault
    sdk: go
  - path: magefiles/lambda
    sdk: go
dependencies:
  - git:
      remote: git@github.com:dagger/cloak.git
      ref: main
      path: examples/yarn/cloak.yaml
  - git:
      remote: git@github.com:dagger/cloak.git
      ref: main
      path: examples/netlify/go/cloak.yaml